# CI red継続対応: 既存Ruff修正差分の整理と安全な反映判断

## 目的
- `/home/main/dev/animaworks-bak` の未コミット差分を読み、**main CI の Lint failure 修正に必要な差分**と**無関係な差分**を切り分ける。
- 必要差分のみで `ruff check core/ cli/ server/` と `ruff format --check core/ cli/ server/` が通ることを再確認する。
- 破壊的 git 操作なしで、次に取るべき安全な反映手順を整理する。

## 既知の前提
- GitHub Actions `main` の最新 failure run は `25902761654`。
- 前回結果 `state/task_results/ci-main-red-20260515.md` で、失敗 job は `unit-tests` の `Lint` step と確認済み。
- その step は `.github/workflows/ci.yml` 上で以下を実行する。
  - `ruff check core/ cli/ server/`
  - `ruff format --check core/ cli/ server/`
- 現在の working tree には以下の差分がある:
  - `core/_anima_heartbeat.py`
  - `core/memory/priming/format.py`
  - `core/memory/rag/repair_rebuild.py`
  - `core/memory/rag/repair_service.py`
  - `core/memory/rag/repair_utils.py`
  - `core/memory/rag/singleton.py`
  - `tests/unit/core/test_background_model_swap.py`

## 制約
- `git reset --hard` / `git restore .` / `git clean -f` 禁止
- 不要なリファクタ禁止
- force push 禁止
- 既存差分を壊さない
- 必要なら machine に読ませるが、最終判断と結果記録は自分で行う

## 調査観点
1. 各差分が Ruff lint/format failure 修正に直接必要かどうかを判定する
2. `core/_anima_heartbeat.py` と `tests/unit/core/test_background_model_swap.py` が別件変更か確認する
3. Ruff対象の 5 ファイルだけで Lint/Format が通る構造になっているか確認する
4. 最新 main CI がまだ赤の理由を、**未反映だから**なのか別理由が混ざるのか、現時点の証拠で整理する
5. 反映手順は「必要差分のみを別コミット化できる状態に整理する」までの材料提示にとどめ、破壊的操作はしない

## machine への期待出力
- 各変更ファイルごとの分類:
  - `required_for_ci_fix`
  - `not_required_for_ci_fix`
  - `uncertain`
- 根拠として `git diff` の要旨を短く添える
- 追加で安全な反映手順案を 3-6 項目に整理する

