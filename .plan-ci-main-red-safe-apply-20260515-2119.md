## Goal

Prepare a safe pre-push handoff for the still-red `main` CI by validating that only the known Ruff-fix files are required, without mixing unrelated heartbeat/test edits and without any destructive git or push operations.

## Evidence already observed

- Latest `main` CI run remains failed: `gh run list -R xuiltul/animaworks --branch main --limit 3` shows run `25902761654` (`Apply priming gate render modes in formatter`) as `failure`.
- `.github/workflows/ci.yml` `Lint` step runs:
  - `ruff check core/ cli/ server/`
  - `ruff format --check core/ cli/ server/`
- Prior task result identified the exact CI-fix candidate files:
  - `core/memory/priming/format.py`
  - `core/memory/rag/repair_rebuild.py`
  - `core/memory/rag/repair_service.py`
  - `core/memory/rag/repair_utils.py`
  - `core/memory/rag/singleton.py`
- Unrelated local edits also exist and must stay out of the CI-fix handoff:
  - `core/_anima_heartbeat.py`
  - `tests/unit/core/test_background_model_swap.py`

## Constraints

- Read current files and prior task results before acting.
- Do not use `git reset --hard`, `git restore .`, `git checkout -- .`, or `git clean -f`.
- Do not push, force-push, or perform any irreversible external action.
- Stop at a human confirmation gate with exact next commands and risks.

## Steps

1. Inspect current repo state (`git status --short`, `git diff --stat`) and confirm only the 7 tracked modified files are present plus old `.plan` artifacts.
2. Re-read the CI workflow and target files to confirm the 5-file/2-file split still matches the current tree.
3. Use `machine` with this plan as input to independently prepare the minimal safe-apply path:
   - verify the 5 CI-fix files are sufficient,
   - exclude the unrelated heartbeat/test changes,
   - create an isolated non-destructive verification copy/worktree/path,
   - run `ruff check core/ cli/ server/` and `ruff format --check core/ cli/ server/`,
   - report exact commands, file list, and risks for handoff.
4. Review machine output against the live repo.
5. If needed, locally re-run the same non-destructive verification to confirm the machine result.
6. Write a new task result summarizing:
   - exact minimal CI-fix file set,
   - current verification evidence,
   - readiness for a human confirmation gate,
   - explicit statement that no push or destructive git operation was performed,
   - exact next safe commands for a human-reviewed commit/push path.
